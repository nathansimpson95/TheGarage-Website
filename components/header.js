/** @jsx jsx */
import { jsx } from '@emotion/core'

export default () => (
  <header css={{ margin: '10px 0' }}>
    <img
      src="../static/logo.svg"
      css={{ width: '40%', maxWidth: 200, margin: 0 }}
      alt="The Garage"
    />
  </header>
)
